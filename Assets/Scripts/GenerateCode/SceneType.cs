using Helper;

namespace Personal.Constant
{
	public enum SceneType
	{
		[StringValue("Boot")]
		Boot,

		[StringValue("Main")]
		Main,

		[StringValue("PreloadScene")]
		PreloadScene,

		[StringValue("Title")]
		Title,
	}
}
