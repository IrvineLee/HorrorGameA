using Helper;

namespace Personal.Constant
{
	public enum SceneType
	{
		[StringValue("Boot")]
		Boot,

		[StringValue("Title")]
		Title,

		[StringValue("Main")]
		Main,

		[StringValue("PreloadScene")]
		PreloadScene,
	}
}
